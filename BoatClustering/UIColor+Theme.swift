/*
See LICENSE folder for this sample’s licensing information.

Abstract:
Utility file that defines constants for colors declared in the asset catalog.
*/

import UIKit

extension UIColor {
    static let reservedColor = UIColor(named: "reservedColor")
    static let partlyBookedColor = UIColor(named: "partlyBookedColor")
    static let availableColor = UIColor(named: "availableColor")
    static let unavailableColor = UIColor(named: "unavailableColor")
}
